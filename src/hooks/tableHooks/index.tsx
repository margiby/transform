export { useScrollSync } from "./useScrollSync";
export { useAutoScroll } from "./useAutoScroll";
export { useTableMetadata } from "./useTableMetadata";

/**
 * Table Hooks - Central export file
 * @module tableHooks
 * @description Dieses Modul bündelt und exportiert alle benutzerdefinierten React-Hooks,
 * die für die Arbeit mit Tabellen in der Anwendung verwendet werden.
 * @exports useScrollSync - Hook zur Synchronisierung des Scrollens zwischen mehreren Tabellen.
 * @exports useAutoScroll - Hook zur automatischen Bildlaufsteuerung in Tabellen.
 * @exports useTableMetadata - Hook zum Abrufen und Verwalten von Metadaten für Tabellen.
 */